# Validation log — re-run on a second cluster (CalculiX 2.23) vs paper (CalculiX 2.21)

> **2026-07-20 — pre-submission audit (`../AUDIT_2026-07-20.md`), corrections applied.** Four entries in
> this file changed as a result and are marked inline: **exp1** (combined buckling 3.985 -> 3.992, and the
> frequency row is a different model), **exp3** (C1 minimum 46 -> 48 plies once the declared frequency
> constraint is actually imposed), **exp5** (closed as stale; the c1 best is 4.67), and the two isotropic
> canaries, which now have a generator (`experiments/exp14_isotropic_canaries.py`) instead of orphan
> `.dat` files — the Rayleigh-Ritz one is **0.7%**, not the 0.8% previously recorded. New in the bundle:
> `exp3b_c1_freq_constrained.py`, `exp14_isotropic_canaries.py`, a Holm step in `exp4`, and a
> figure canary in `figures/fig_validation_parity.py` that checks the printed percentages, not only the
> loads (it immediately caught A1 aluminium 1936 -> 1937).

The bundle code was re-run on a 32-core compute node of a second cluster (workers
the worker fleet is woken on demand but unreliable — the nodes auto-suspend when idle, so the reliable runs
on the same fabric). Each result is compared to the manuscript, which used CalculiX 2.21.

| Exp | What | Paper (ccx 2.21) | Re-run (ccx 2.23) | Verdict |
|-----|------|------------------|-------------------|---------|
| exp2 | 60-ply cross-ply baseline | ~~C1 14.63 C2 3.59 C3 2.41, f 614.7~~ (does NOT reproduce) | ccx 2.21 (laptop) **and** 2.23 (cluster): C1 15.27 C2 3.71 C3 2.30, f 628 | **CLOSED 2026-07-22:** the 14.63 / 614.7 column was never a ccx-version drift and never an "older deck config": it was **the other cross-ply in this bundle**. `fe/reference_cases.py::validation_anchor` used `[0,90]*30` (ANTIsymmetric) whilst `exp2_crossply_baseline.py` and the manuscript use `[0/90]_15s` (symmetric), and both were compared against the same Abaqus reference. Measured side by side: `[0/90]_15s` -> C1 15.2735, f1 627.97 Hz (0.2% from Abaqus' 627); `[0,90]*30` -> C1 14.6273, f1 614.68 Hz (1.9%). `validation_anchor` has been aligned to the symmetric stack, so the bundle now has ONE definition of the anchor and reproduces exp2 exactly (15.2735 / 3.7068 / 2.3007 / 627.97). vs Giacomo's Abaqus (15.22/3.70/2.47/627): C1/C2/f agree ~0.3%; C3 2.30 vs 2.47 (6.9%, hardest case). Cross-ply is a validation anchor, NOT a manuscript number. |
| exp3 | min-ply designs (set2) | ~~C1 46/4.28~~, C2 50/4.21, C3 54/4.38 | C2/C3 identical; **C1 SUPERSEDED** | **PARTIAL — audit A1 (2026-07-20):** exp3 never evaluated the frequency, although the paper declares f1>600 Hz as Constraint 4. The 46-ply C1 designs sit at 590.8 (set1) / 590.2 Hz (set2): infeasible. C1 is now **48 plies** (set1 BF 4.9136 f1 618.57; set2 BF 4.8955 f1 617.43), regenerated by `exp3b_c1_freq_constrained.py` at the same budget. C2/C3 unaffected (617/671 Hz). |
| exp3 | 6 explicit sequences, both alphabets | — | produced, feasible Q~1.5e-3 | PASS -> in paper (tab:explicitseq) |
| exp4 | 30-seed comparison (set2, N=40) | "close, no winner, no stat. claim" | KW p<0.05 all 3 cases; ACO/PSO strong, GA weak on shear/combined; no universal winner | PASS + enriched |
| exp5 | alphabet contrast (set1 vs set2, best BF) | "extended alphabet: no advantage" (§3.4) | ~~c1 best 4.6439->4.6677; c2 +11.9% (2.26->2.53); c3 +6.0% (2.11->2.24); on the MEAN c1 +22.7% (3.358->4.119)~~ **read off the stale `exp5_alphabet_set1.json`** | **CLOSED as stale 2026-07-20; the numbers in this row CORRECTED 2026-07-25.** The "no advantage" claim is no longer in the manuscript (0 hits since v4), and the mechanism (search-space connectivity under the 45-deg rule) is in §alphabet. But the figures previously in this row came from `data/exp5_alphabet_set1.json`, which is a legacy artefact of an earlier revision and is **not** what the paper is on. Recomputed from `data/exp4_optimiser_comparison.json` (the valid record, keys `set1`/`set2`): c1 best **4.6468 -> 4.7222**, c2 **2.2627 -> 2.5391**, c3 **2.1309 -> 2.2016**; on the seed-mean c1 **3.3316 -> 4.0984 = +23.0%** (GA +17.1%, ACO +18.8%, PSO +34.9%), c2 +10.2%, c3 +6.9%. Every one of these matches the manuscript to the printed digit. |
| exp6 | Haftka-Walsh benchmark | 12/12 max-buckling <0.01%, 8/8 min-thk | worst 0.01%, 8/8 match | PASS (exact) |
| exp7 | FE certification (exhaustive N=8) | GA within 0.51% of global | gap 0.51-0.59% | PASS (exact) |
| exp8 | buckling-evaluator robustness | non-monotonic BF is real D(z) physics, lowest mode governs | reproduced | PASS (exact) |
| exp9 | **vs ESPERIMENTI PUBBLICATI** (NASA TP-3007, Nemeth 1990: 6 laminati graphite-epoxy + 1 piastra Al, compressione fino a buckling) | — (nuovo, 2026-07-14) | **medio 4.3%, peggiore 7.1%** su 7 carichi critici MISURATI; l'analisi dell'autore sugli stessi provini fa **5.4% medio, 13.6% peggiore** | **PASS — il nostro FE segue l'esperimento meglio dell'analisi della fonte.** Riprodotto su DUE host (laptop ccx 2.21 e nodo di calcolo ccx 2.21, 32 core): numeri identici alla cifra stampata. Esegui su un nodo di calcolo con `CCX_REMOTE=<host> ./run_on_head.sh experiments.exp9_experimental_validation`. Canarini: (a) piastra ortotropa appoggiata vs forma chiusa CLPT −1.18%; (b) piastra isotropa appoggiata vs k=4.0 esatto −1.4%; (c) isotropa incastrata vs Rayleigh−Ritz indipendente (k=6.743) −0.8%. **Anomalia risolta:** il valore analitico dell'alluminio della fonte (1773 lb) e' 9.2% sotto l'esatto perche' usa lo spessore NOMINALE del foglio (0.0625 in), non lo 0.0647 in misurato che lui stesso riporta in Tab.8 — non e' un errore nostro. **Attenzione (t^3):** per B1 la fonte usa H=0.110 in nei conti ma dichiara 0.107 in misurato: 3 millesimi di pollice = 9% di carico (nostro scarto −0.9% con H, −8.7% con lo spessore misurato). L'incertezza sperimentale e' dominata dallo spessore. |
**AGGIORNAMENTO 2026-08-26 — la riga della frequenza e' stata SOSTITUITA.** I 41.96/41.91 Hz venivano da un crop a 928 nodi del modello originale del coautore, che non e' in questo bundle: nessun lettore poteva ricalcolarli (rilievo A3, qui sotto). La ricostruzione della geometria e' rimasta ritirata: e' un parametro libero fittato su un numero. Al suo posto il confronto modale su un deck che il bundle genera, il pannello S8R a 661 nodi e 60 ply: **ccx 2.21 532.91 Hz contro Abaqus 2026 Learning Edition 531.69 Hz, 0.23%**, e i primi cinque modi entro **1.9%** (0.23 / 1.39 / 1.10 / 0.03 / 1.93). Misurato da `exp29_modal_cross_solver.py`, col `.dat` di Abaqus versionato accanto. Nota: 661 e 928 stanno entrambi sotto i 1000 nodi della Learning Edition, che e' con ogni probabilita' il motivo per cui il crop del coautore era di quella dimensione.

⚠️ **SECONDO AGGIORNAMENTO, 2026-08-26 (pomeriggio): anche le DUE RIGHE DI BUCKLING sono state rieseguite.** La riga qui sotto dice ancora «combined 3.9152 -- matches the reference to the printed digit» e «2.0%, not 1.8%»: **superata**. Il lato Abaqus e' stato rifatto in LE 2026 sul deck che il bundle genera (`make_ccx_deck`), non sulla variante `rr_shell_composite` del 04/07 che non e' piu' qui: **assiale 10.819 identico, combinato 3.9208**. Lo scarto sul combinato e' quindi **1.83%**, non 1.98%, ed e' quello che il paper stampa. I due file di risultati Abaqus sono versionati (`data/exp1_abaqus_axial661.dat`, `data/exp1_abaqus_combined661.dat`), come per la riga modale.

📌 **La differenza dello 0,15% fra 3.9152 e 3.9208 e' fra DUE GENERATORI DI DECK, a parita' di solutore e di versione** (entrambi Abaqus LE 2026). E' il motivo per cui vale la pena riportare la coppia misurata sul deck che il bundle produce: cosi' l'affermazione del paper che i due solutori vedano lo stesso input e' verificabile invece che da prendere sulla fiducia.

| exp1 | ccx vs Abaqus validation | ~~ccx 10.84/**3.985**/41.96~~ **-> ccx 10.84/3.992/41.96** vs Abq 10.819/3.915/41.91. **Audit A2 (2026-07-20):** the combined factor recomputed from scratch on the 661-node deck is **3.9924**, not 3.985; the inherited 3.985 predates the bundle (`git log -S"3.985"`) and had never been recalculated. The difference vs Abaqus is therefore **2.0%**, not 1.8%. The axial row reproduces exactly (10.8395). **Audit A3:** the frequency row is NOT on the same deck (928-node 3-ply crop); the ~356 mm reconstruction below is withdrawn as a one-free-parameter back-derivation with no residual power to verify. | **RESOLVED 2026-07-04** — re-run on Abaqus LE 2026 on the SAME 661-node deck (`rr_shell_composite.py` abq variant): **axial 10.819, combined 3.9152** = matches the Abaqus reference to the printed digit; ccx side 10.84/3.985 reproduced locally (ccx 2.21) on the same deck. Frequency row RECONSTRUCTED (hypothesis, pending G.C.): the 100×50 buckling coupon gives 533 Hz, NOT 41.96; the SAME 6mm laminate on a ~356mm planform gives ccx 41.90 / Abaqus LE 41.878 Hz (**0.04% cross-solver**) ≈ paper 41.96/41.91 → the modal deck is a longer/larger (industrial) panel, not the buckling coupon (h/L² degenerate, exact geometry pending G.C.) | **PASS (buckling); freq cross-solver OK on reconstruction** |

## Findings to act on (manuscript)

> Re-checked one by one on 2026-07-25. Four of the five were already closed by later work and were
> still listed here as open; one of them, worse, asked for a change that would have put a WRONG number
> in the manuscript. Only #2 survives, and it is a "could", not a defect.

1. ~~**exp5 — the c1 best is 4.67, not the 4.65 the paper printed**~~ **WITHDRAWN 2026-07-25: the
   paper was right and this finding was wrong.** The 4.67 was read off the stale
   `exp5_alphabet_set1.json`. The valid record, `exp4_optimiser_comparison.json`, gives a c1 set1 best
   of **4.6468**, which is the 4.65 the paper prints. Acting on this finding would have introduced an
   error. The rest of the entry (the "no advantage" wording, removed at v4) was already closed.
2. **exp4 — refine** (still open, optional): with 30 seeds the GA/ACO/PSO differences are statistically
   significant (KW p<0.05 in all three cases; GA weakest on shear/combined), though no optimiser wins
   all cases. The paper's "no statistical claim / close" could be upgraded to a powered statement.
3. ~~**exp2 — ccx version drift**~~ **CLOSED 2026-07-22**, see the exp2 row above: the 14.63 / 614.7
   column was never a version drift, it was the *other* cross-ply in the bundle (antisymmetric
   `[0,90]*30` against symmetric `[0/90]_15s`). `validation_anchor` has been aligned and the bundle
   reproduces exp2 exactly on ccx 2.21.
4. ~~**load-table mismatch**: paper tab:buckling (C1 axial -1000) vs code CASES (C1 axial -2400)~~
   **CLOSED — verified 2026-07-25 on both sides:** `tab:buckling` prints $-2400$ / $4900$ / $-1500$ and
   `optimisers/constrained_search.CASES` carries the same three, digit for digit.
5. ~~**import bug**: rr_interlaminar `from frd_parse import`~~ **CLOSED — verified 2026-07-25:** no
   such import exists anywhere in `code/` any more (this file was its last mention).

## Compute notes
- The control node (ccx 2.23, conda env) is the reliable one. The worker fleet auto-suspends when idle and the
  the environment replication was incomplete on the second host; not worth fighting for this workload size.
